meta {
  name: /api/profile
  type: http
  seq: 4
}

post {
  url: {{localhost}}/api/profile
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  name: RvnSytR
  ~image: @file()
}

settings {
  encodeUrl: true
}
