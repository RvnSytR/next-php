meta {
  name: /api/me
  type: http
  seq: 7
}

post {
  url: {{localhost}}/api/me
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  name: RvnSytR
  ~image: @file()
}

settings {
  encodeUrl: true
}
