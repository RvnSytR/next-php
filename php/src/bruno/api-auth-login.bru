meta {
  name: /api/auth/login
  type: http
  seq: 3
}

post {
  url: {{localhost}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "example@example.ex",
    "password": "Example#123"
  }
}

settings {
  encodeUrl: true
}
